<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.focusmedi.app">

    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <!-- POST_NOTIFICATIONS (from Android 13) should be requested at runtime -->

    <application
        android:label="Focusmedi"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher">

        <!-- MainActivity will be generated by Flutter; we include a Kotlin skeleton in src/ -->

        <!-- Accessibility service declaration placeholder -->
        <service
            android:name="com.focusmedi.app.FocusAccessibilityService"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE"
            android:exported="false">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_service_config" />
        </service>

        <!-- Foreground focus service -->
        <service android:name="com.focusmedi.app.FocusService" android:exported="false"/>

    </application>
</manifest>
